/** Типы результатов админ-действий (используются клиентскими компонентами). */
export type SetArchivedResult = { archived: boolean };
export type DeleteResult = { cleanedVideos: number; cleanedFiles: number };
